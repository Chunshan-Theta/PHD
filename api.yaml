swagger: "2.0"
info:
  description: "for PHD"
  version: "1.0.0"
  title: "PHD"
  termsOfService: ""
  contact:
    email: "apiteam@swagger.io"
  license:
    name: "Apache 2.0"
host: "127.0.0.1"
basePath: "/PHD"
tags:
- name: "member"
  description: "user"

- name: "step"
  description: "Step"

schemes:
- "https"
- "http"
paths:
  /groupmembers:
    get:
      tags: 
        - "member"
      summary: ""
      description: "" 
      responses:
        200:
          description: OK
          schema:
            properties:
              members:
                type: array
                items:
                  $ref: '#/definitions/member'
  /members/{mid}:
    get:
      tags: 
        - "member"
      summary: ""
      description: "" 
      parameters: 
      - name: mid
        type: string
        in: path
        required: true
      responses:
        200:
          description: OK
    
    
  


  
definitions:
  step:
    type: "object"
    properties:
      sid:
        type: string
      group:
        type: string
      
      title:
        type: string
      deadline:
        type: string
      log:
        type: string
      status:
        type: boolean
  member:
    type: "object"
    properties:
      mid:
        type: string
      account:
        type: string
      pws:
        type: string
      name:
        type: string
      description:
        type: string
      group:
        type: string
      permission:
        type: string
      steps:
        type: array
        items:
          $ref: '#/definitions/step'
      alert:
        type: boolean
      hidden:
        type: boolean

  
